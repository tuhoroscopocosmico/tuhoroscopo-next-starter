'use client';

// Este archivo ya no es la experiencia activa.
// /checkout2 redirige a /checkout (ver checkout2/page.tsx).
// Se conserva como referencia histórica.

export default function Checkout2Content() {
  return null;
}
